import 'package:flutter/material.dart';

// 这也是留在这里占位置用的, 其实根本还没用上
class RecentTripPage extends StatefulWidget {
  const RecentTripPage({super.key});

  @override
  State<RecentTripPage> createState() => _RecentTripPageState();
}

class _RecentTripPageState extends State<RecentTripPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(), 
    );
  }
}